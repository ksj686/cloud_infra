# 어제 나눈 대화 반영하도록 하기

#### 로그 파일 삭제 어떤 정책을 따를지 - 주기적으로 삭제하지 않으면 저장소 초과될 여지가 있음. 자동 삭제? 주기는 어떻게? 백업은 어떻게?

#### S3는 굳이 여러개 필요 없는지. 그리고 S3는 기본적으로 HA인지, 너무 많은 사용자가 있어도 S3는 영향이 없는지

#### 시스템상에 문제가 있을때 문제를 우회하여 해결할 수 있는 전략이 있어야한다. dynamo DB나 S3는 기본적으로 HA 고가용성

#### 수직 확장 - 인스턴스 크기를 키우는 방법 => 단일 장애지점이 여전히 남아있는 구조이므로 권장X

#### 수평확장 - 인스턴스 개수 증가 => 보통 수평 확장을 권장. 가격도 더 쌈.

#### 등록한 키 삭제(?) - 보안상 문제되는 부분 없도록 삭제하는 작업

#### DB 이중화 / DB 마스터 슬레이브 또는 병렬연결 <= 이번 프로젝트에 포함시켜야하는 내용

### 프록시 SQL - 쿼리에 따라 마스터, 슬레이브를 나눠준다

#### CI/CD 개념에서 새로운 버전 배포시 문제 없을 경우 클러스터상의 다른 서버에 순차적으로 자동으로 배포.

#### 문제 발생과는 무관하게 관리자가 이전 버전으로 roll back 하기 쉽도록 세팅(새 버전의 기능을 다시 빼기로 결정난 경우, 또는 CI/CD 상에서 발견되지 않는 오류 발생한 경우에 해당)

#### gpu 관련 라이브러리가 쓰인 python 웹을 클러스터 상의 특정 노드(gpu 성능이 좋은)에서 실행시키거나, 반대로 gpu 성능이 안좋은 노드에서 실행되지 못하도록 설정 - Taints와 Tolerations / Node Selectors와 Affinity

---

#### 온프레미스랑 클라우드를 같은 클러스터로 돌려야될 경우 온프레미스에서 minIO를 사용하면 환경설정값이 다른데 코드가 같아도 온프레미스일때 적용하는 값과 AWS 에 적용하는 값을 다르게 설정하는 구조적 문제 해결 방법?

1. 왜 코드는 같아도 되는가? (표준화된 인터페이스)
   우리가 사용하는 모든 S3 SDK(Boto3, aws-sdk 등)는 설계부터 Endpoint를 외부에서 주입받도록 되어 있습니다.

- 코드 내부: s3_client = create_s3_client(endpoint=os.environ['S3_ENDPOINT'])
- 온프레미스 주입: S3_ENDPOINT 환경 변수에 minio.local:9000 전달
- AWS 주입: S3_ENDPOINT 환경 변수에 s3.ap-northeast-2.amazonaws.com 전달

애플리케이션은 "S3 프로토콜을 사용한다"는 사실만 알면 되지, 그게 내부 서버인지 외부 서버인지 알 필요가 없습니다.
이것이 바로 현업에서 말하는 '운영 환경의 추상화'입니다.

2. 이 차이를 누가 관리하는가? (운영 체계)
   이 관리는 개발자가 코드 안에서 if(온프레미스){...} 식으로 처리하지 않습니다. 인프라/운영 환경(K8s)이 주입합니다.

- ConfigMap / Secret (K8s):
  - 온프레미스 K8s 클러스터의 values-onprem.yaml에는 MinIO 엔드포인트가 명시되어 있습니다.
  - AWS EKS(클라우드 K8s)의 values-cloud.yaml에는 AWS S3 엔드포인트가 명시되어 있습니다.
- 배포 파이프라인 (Argo CD):
  - Argo CD는 Git 저장소의 같은 코드를 사용하지만, 환경별로 다른 values.yaml 파일을 적용(Sync)합니다.
  - 결과적으로 똑같은 파드(Pod) 이미지가 배포되지만, 환경에 따라 서로 다른 환경 변수를 달고 뜹니다.

3. 왜 이렇게 하는 것이 '쉬운가'?
   과거에는 서버마다 설정 파일(.xml, .conf)을 일일이 수정했기 때문에 매우 어려웠습니다. 하지만 현재 우리가 구축하고 있는
   GitOps + Helm 조합에서는 다음과 같이 간단해집니다.

1. Git 저장소: charts/my-app/values.yaml (템플릿)
1. Git 저장소: values/onprem.yaml (엔드포인트: minio...)
1. Git 저장소: values/cloud.yaml (엔드포인트: s3.amazon...)
1. 배포: helm install my-app -f values/onprem.yaml (이 명령 한 줄로 끝납니다.)
